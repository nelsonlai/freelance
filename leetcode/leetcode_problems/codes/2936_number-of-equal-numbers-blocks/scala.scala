/**
 * Definition for BigArray.
 * class BigArray(elements: Array[Int]) {
 *     def at(Long): Int
 *     def size(): Long
 * }
 */
object Solution {
    def countBlocks(nums: BigArray): Int = {
        
    }
}